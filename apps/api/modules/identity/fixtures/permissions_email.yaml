# Permission codes for the Email Notification (SMTP) configuration module.
- { code: org:email_config:read,  description: Read email notification (SMTP) configuration }
- { code: org:email_config:write, description: Edit email notification (SMTP) configuration, dangerous: true }
